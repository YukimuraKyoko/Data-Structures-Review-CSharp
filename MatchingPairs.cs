using System;

// We don’t provide test cases in this language yet, but have outlined the signature for you. Please write your code below, and don’t forget to test edge cases!
class MatchingPairs
{
    /*
    static void Main(string[] args)
    {
        // Call matchingPairs() with test cases here
    }

    private static int matchingPairs(string s, string t)
    {
        // Write your code here
        //Algorithm:
        //Iterate both s and t
            //Store all matching pairs into a Counter
            //CASE 1: If reached end without any unmatching pairs, which means both strings match exactly
                //subtract string's length by 2, output (string's length - 2)
            //CASE 2: If unmatching pairs are found, but can be switched to add 2 more to count, add 2 to count
                //How to find this: Example: s="abcd", t="adcb"
                //int a;
                //int b;
                //s[1] == t[3]
                //s[3] == t[1]
                //if(s[a] == t[b] && s[b] == t[a]){
                //  Count+=2;
                //}
            //CASE 3: Example: s= "abcf", t= "abcd": only 1 unmatching pair, subtract count by 1, because you still have to switch 2

        return 0;
    }
    */
}